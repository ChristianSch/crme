{
  "name": "crme-linkedin-extension",
  "description": "Browser extension to capture LinkedIn profiles and companies to CRME",
  "private": true,
  "license": "PolyForm-Noncommercial-1.0.0",
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "dev:firefox": "wxt -b firefox",
    "build": "wxt build && npm run export:chrome",
    "build:firefox": "wxt build -b firefox",
    "export:chrome": "rm -rf dist/chrome-mv3 dist/crme-linkedin-extension-chrome.zip && mkdir -p dist && cp -R .output/chrome-mv3 dist/chrome-mv3 && cd dist && zip -qr crme-linkedin-extension-chrome.zip chrome-mv3",
    "zip": "wxt zip",
    "zip:firefox": "wxt zip -b firefox",
    "compile": "vue-tsc --noEmit",
    "postinstall": "wxt prepare"
  },
  "dependencies": {
    "vue": "^3.5.25"
  },
  "devDependencies": {
    "@wxt-dev/module-vue": "^1.0.3",
    "typescript": "^5.9.3",
    "vue-tsc": "^3.1.8",
    "wxt": "^0.20.6"
  }
}
